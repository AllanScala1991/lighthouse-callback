{
  "name": "@cypress-tests/cypress-light-callback",
  "version": "2.0.0",
  "main": "dist/lighthouse.service.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "tsc --build",
    "prepare": "yarn run build",
    "version": "git add .",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "cypress-audit": "^1.0.0",
    "typescript": "^4.3.5"
  }
}
